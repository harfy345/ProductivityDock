<Window x:Class="ProductivityApp.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Height="320"
        Width="420"
        WindowStartupLocation="CenterScreen"
        Background="#121212"
        WindowStyle="None"
        AllowsTransparency="True">

    <Border Background="#1E1E1E"
            CornerRadius="16"
            Padding="20"
            BorderBrush="#2A2A2A"
            BorderThickness="1">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <TextBlock Text="⚙ Settings"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <!-- SESSION -->
            <StackPanel Grid.Row="1" Margin="0,0,0,15">

                <TextBlock Text="Session Duration (minutes)"
                           Foreground="LightGray"
                           Margin="0,0,0,5"/>

                <TextBox x:Name="SessionBox"
                         Height="35"
                         Background="#2A2A2A"
                         Foreground="White"
                         BorderBrush="#3A3A3A"
                         Padding="8"
                         BorderThickness="1"/>
            </StackPanel>

            <!-- COOLDOWN -->
            <StackPanel Grid.Row="2" Margin="0,0,0,20">

                <TextBlock Text="Cooldown Duration (minutes)"
                           Foreground="LightGray"
                           Margin="0,0,0,5"/>

                <TextBox x:Name="CooldownBox"
                         Height="35"
                         Background="#2A2A2A"
                         Foreground="White"
                         BorderBrush="#3A3A3A"
                         Padding="8"
                         BorderThickness="1"/>
            </StackPanel>

            <!-- BUTTONS -->
            <Grid Grid.Row="3">

                <Grid.Resources>

                    <!-- CANCEL BUTTON STYLE -->
                    <Style x:Key="CancelButtonStyle" TargetType="Button">
                        <Setter Property="Background" Value="#2E2E2E"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="10">
                                        <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>

                        <Style.Triggers>
                            <!-- NORMAL HOVER -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3F3F3F"/>
                            </Trigger>

                            <!-- CLICKED -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1F1F1F"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <!-- SAVE BUTTON STYLE -->
                    <Style x:Key="SaveButtonStyle" TargetType="Button">
                        <Setter Property="Background" Value="#3A7AFE"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="FontWeight" Value="Bold"/>

                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="10">
                                        <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>

                        <Style.Triggers>
                            <!-- HOVER -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5A90FF"/>
                            </Trigger>

                            <!-- CLICK -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2F6EEB"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                </Grid.Resources>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- CANCEL -->
                <Button Content="✖ Cancel"
            Grid.Column="0"
            Margin="0,0,10,0"
            Height="40"
            Style="{StaticResource CancelButtonStyle}"
            Click="Cancel_Click"/>

                <!-- SAVE -->
                <Button Content="💾 Save Settings"
            Grid.Column="1"
            Height="40"
            Style="{StaticResource SaveButtonStyle}"
            Click="Save_Click"/>

            </Grid>

        </Grid>

    </Border>

</Window>